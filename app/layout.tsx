import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Typography Analyzer - Interactive Web Typography Tool",
  description:
    "Analyze and explore the complete typography of any website with interactive highlighting and detailed JSON data.",
  keywords: ["typography", "web design", "fonts", "css", "analysis"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
